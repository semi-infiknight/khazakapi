/** Public site origin */
export const SITE_ORIGIN = "https://qazaq.up.railway.app";

export const SITE_NAME = "Qazaq Stack";

export function formatCatalogueCaption(total = 688) {
  return `${total} Kazakhstan Native Endpoints in the catalogue.`;
}
